<div class="container">
  <section>
    <h1>CARTO Support Center</h1>
    <p>
      Please 
      <a href="/hc/en-us/signin?return_to=https%3A%2F%2Fcartoteam.zendesk.com%2Fhc%2Fen-us%2Frequests&locale=en-us">
        sign-in 
      </a> to get access
      <a href="/hc/en-us/requests">
        your cases
      </a>.
    </p>
  </section>
</div>

<script>
  function getCookie(name) {
    var dc = document.cookie;
    var prefix = name + "=";
    var begin = dc.indexOf("; " + prefix);
    if (begin == -1) {
      begin = dc.indexOf(prefix);
      if (begin != 0) return null;
    }
    else {
      begin += 2;
      var end = document.cookie.indexOf(";", begin);
      if (end == -1) {
        end = dc.length;
      }
    }
    // because unescape has been deprecated, replaced with decodeURI
    //return unescape(dc.substring(begin + prefix.length, end));
    return decodeURI(dc.substring(begin + prefix.length, end));
  }

  document.addEventListener('DOMContentLoaded', function () {
    const cookie = getCookie('_zendesk_authenticated');

    if (cookie) {
      console.log(cookie);
      console.log('authenticated!')
    }
  });
</script>